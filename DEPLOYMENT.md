# راهنمای ساخت خروجی (Build) و انتشار (Deployment) پروژه Vexora Tools

این پروژه یک وب‌اپلیکیشن React با TypeScript و Tailwind CSS است که کاملاً مستقل از سرور (Client-side) کار کرده و به راحتی روی تمام سرویس‌های میزبانی استاتیک منتشر می‌شود.



## 🛠️ ۱. تست و ساخت فایل خروجی به‌صورت محلی (Local Build)

برای اطمینان از صحت کدهایی که اضافه شده‌اند و ساخت فایل نهایی پروژه:

```bash
# نصب پکیج‌ها (در صورت نیاز)
npm install

# ساخت نسخه Production
npm run build

# پیش‌نمایش نسخه نهایی ساخته شده روی سیستم
npm run preview
